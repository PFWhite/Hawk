{
  "name": "hawk",
  "version": "0.0.1",
  "description": "A frontend component registry with async loading",
  "main": "server/app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Patrick White",
  "license": "ISC",
  "dependencies": {
    "express": "^4.14.1",
    "nunjucks": "^3.0.0"
  }
}
